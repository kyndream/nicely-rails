<%= form_for(transaction) do |f| %>
  <% if transaction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(transaction.errors.count, "error") %> prohibited this transaction from being saved:</h2>

      <ul>
      <% transaction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :transactionable_id %>
    <%= f.text_field :transactionable_id %>
  </div>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :notes %>
    <%= f.text_field :notes %>
  </div>

  <div class="field">
    <%= f.label :amount %>
    <%= f.text_field :amount %>
  </div>

  <div class="field">
    <%= f.label :due_on %>
    <%= f.date_select :due_on %>
  </div>

  <div class="field">
    <%= f.label :recur %>
    <%= f.text_field :recur %>
  </div>

  <div class="field">
    <%= f.label :recur_type %>
    <%= f.number_field :recur_type %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
